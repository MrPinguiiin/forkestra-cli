{
  "name": "@forkestra-cli/cli",
  "type": "module",
  "bin": {
    "forkestra": "./src/index.ts"
  },
  "exports": {
    ".": {
      "default": "./src/index.ts"
    }
  },
  "scripts": {
    "build": "tsc -b",
    "check-types": "tsc -b",
    "compile": "bun build --compile --minify --bytecode ./src/index.ts --outfile forkestra"
  },
  "dependencies": {
    "@forkestra-cli/core": "workspace:*",
    "@forkestra-cli/db": "workspace:*",
    "commander": "^15.0.0",
    "dotenv": "catalog:",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@forkestra-cli/config": "workspace:*",
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
